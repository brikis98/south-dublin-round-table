# Project Agent Rules

For any HTML/CSS changes in this project:

1. Never use inline styles (`style="..."`).
2. Use BassCSS utility class names whenever fitting ones exist.
3. If no BassCSS class exists for the needed behavior, define an immutable class in `assets/css/main.css` and use that class in the HTML.
